<%= simple_form_for @product, validate: true do |f| %>
  <%= f.input :id_category, label: 'Categoria' do %>
    <%= f.select :id_category, Category.all.map { |r| [r.name, r.id_category] } %>
  <% end %>
  <%= f.input :name, label: 'Nombre del producto', required: true %>
  <%= f.input :description, label: 'Descripción'%>
  <%= f.input :price, label: 'Precio', required: true %>
  <%= f.button :submit, "Guardar" %>
<% end %>
